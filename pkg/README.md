# pkg 模块缓存占位

## 作用
- 保留 Go 默认的 `pkg/mod/cache/lock` 目录结构，避免某些工具期望目录存在而报错。
- 不承载业务代码，Go 依赖仍由模块下载后写入本地缓存。

## 使用提示
- 无需手工编辑；如需清理依赖，可使用 `go clean -modcache`，与本目录无强耦合。
- 若在 CI/打包中不希望提交，可在 `.gitignore` 中排除，但当前保留空文件以提示用途。
